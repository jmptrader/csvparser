/*
package CSVParser parses the CSV file and pass the items of each row(line) to user's own function to process the data.

To do this, apps that use CSVParser should implement the DataProcessor interface.
*/
package csvparser
